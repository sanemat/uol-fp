---
Q1: What metadata does MOLE extract from a paper?
Give three example attributes and describe what type of answer each expects.

A:

---
Q2: MOLE uses a schema to guide the LLM. What are the five keys in the schema?
How does the "options" key work?

A:

---
Q3: The paper uses four types of validation on the LLM output.
Name them and explain what each checks.

A:

---
Q4: MOLE processes entire documents. What input formats does it support?
How does it handle PDFs differently from LaTeX?

A:

---
Q5: Connection to proto3

MOLE and proto3 both use a schema-driven prompt to extract structured output from full papers.
What does MOLE do that proto3 does not? What does proto3 do that MOLE does not?

A:

---
