---
Q1: The paper tests LLMs on four IE tasks. What are the four tasks?

A:

---
Q2: The main finding is that LLMs are worse than fine-tuned small models on few-shot IE.
Under what condition do LLMs win? Under what condition do small models win?

A:

---
Q3: What does "hard samples" mean in this paper?
How does the paper measure whether a sample is hard or easy?

A:

---
Q4: What is the filter-then-rerank paradigm?
Which model acts as the filter, and which acts as the reranker?

A:

---
Q5: Connection to proto3

proto3 does not do few-shot IE — it sends the full paper and asks for one answer per role.
Does this paper's finding (LLMs are bad at few-shot IE) apply to proto3? Why or why not?

A:

---
