[project]
name = "uol-fp-report4"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "markdown",
    "pygments",
    "weasyprint",
]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
