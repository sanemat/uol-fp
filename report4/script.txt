VIDEO SCRIPT (about 3 minutes; spoken text approximately 400 words)
Demo paper: "Attention Is All You Need" (Transformer)

Files to have open before recording:
- PDF and GROBID-generated TEI XML for "Attention Is All You Need"
- Notebook: proto3/3pipeline.ipynb (setup cells already run)
- Saved baseline output: proto3/baseline/transformer.json
- Saved Variant B output: proto3/results_b/transformer.json
- Report: Table 1, Figure 3, Figure 7, Tables 9, 11, 13 and 14

================================================================
[0:00-0:12]  1. Introduction
DO:  Show the PDF beside report Table 1 (the four-role profile).
SAY: Hello. This project extracts research methodologies from a computing
     paper: technical method, task, dataset, and evaluation metric. It is
     designed to support students' first pass of a literature review.

[0:12-0:30]  2. Problem and design choice
DO:  Scroll the PDF. Show report Figure 3 (proto2 versus proto3).
SAY: My first prototype, proto2, classified sentences one by one. On the
     Transformer paper, it returned 160 candidate sentences for the metric
     but none for the task or dataset. Proto3 instead reads the cleaned
     document and returns one answer with evidence for each role.

[0:30-0:50]  3. PDF to XML (conversion done before recording)
DO:  Show the PDF beside its GROBID-generated TEI XML; point to a section
     heading in the XML.
SAY: This is "Attention Is All You Need" as a PDF, and this is the XML
     produced by GROBID. I converted it before recording. The XML keeps
     the section structure needed for extraction and evidence.

[0:50-1:10]  4. Stage 0 and Stage 1
DO:  In proto3/3pipeline.ipynb, show Stage 0 and Stage 1 and their document
     text output. Run the cells only if their prerequisites are ready.
SAY: Stage 0 reads the XML. It skips references, acknowledgements, and
     empty sections. Stage 1 joins the remaining sections in reading
     order. Unlike proto2, proto3 does not split the text into sentences
     or apply a per-sentence acceptance threshold.

[1:10-1:40]  5. Stage 2: schema-guided extraction
DO:  Show the three prompt rules, the Pydantic model, and its validator.
     Run Stage 2c if the API is ready. Do not assume this new run will
     reproduce the saved baseline output.
SAY: Stage 2 sends the document to Gemini. The prompt says to use the
     authors' own method, return null for a missing role, and quote the
     paper word for word. A Pydantic model supplies the JSON schema.
     Its validator requires the answer and evidence to be present
     together, or both null. This checks the output structure, not
     whether an answer is correct. The call uses temperature zero
     and seed zero, but repeated results can still differ.

[1:40-2:05]  6. Saved result and evidence
DO:  Open proto3/baseline/transformer.json (report Figure 7). Make it clear
     this is the SAVED baseline, not necessarily the live run. Point to
     each answer and its section and quote.
SAY: This is the saved baseline result in my report. It identifies
     Transformer as the method, machine translation as the task,
     WMT 2014 English-German as the dataset, and BLEU as the metric.
     Each answer includes a section and a quotation that the reader
     can check against the paper.

[2:05-2:30]  7. Variant B: decomposed extraction
DO:  Show Stage 2d, then report Table 13. Optionally show report Table 9
     to illustrate that Transformer's Task answer changed.
SAY: Variant B makes four separate calls, one per role. In this
     single-run comparison, Dataset F1 rose from 0.91 to 1.00, while
     Task F1 stayed at 0.33. But the two versions matched different
     papers for Task, so the score hides a change in answers.

[2:30-3:00]  8. Results, limits, and conclusion
DO:  Show report Table 11, then Table 14 or Section 6. Finish on the
     repository page: https://github.com/sanemat/uol-fp
SAY: I evaluated six papers against gold labels I wrote myself.
     Across five runs, method F1 was 0.83, Dataset F1 was 0.91,
     and Task F1 was 0.33. The Task pilots did not show a reliable
     improvement. For example, a reasonable answer can fail a
     simple substring match because it uses different wording.
     Also, a valid JSON answer can still have weak evidence.
     The limits are six papers, one annotator, and no user study.
     Future work includes semantic rescoring and a larger corpus.
     The code is on GitHub. Thank you.

================================================================
RECORDING NOTES
- The four role values in block 6 belong to the SAVED baseline (Figure 7).
  Label a live Stage 2c result separately; do not narrate it as the saved result.
- Dataset F1 0.91 is the mean across five runs in Table 11 and Variant A's
  one-run value in Table 13. The frozen baseline in Table 10 has Dataset F1 0.73.
- Variant B's Dataset F1 1.00 and Task F1 0.33 come from Table 13; each variant
  was run once for that comparison.
